template_version: 0.6
id: custom-1781473016387-igpc76
title: Bitbucket Project Secret Scanning Allowlist Added
description: Detects when a secret scanning allowlist rule is added for projects.
status: test
logsource:
  scope: specific
  category: process_creation
  product: bitbucket
  service: audit
level: low
detection:
  kql_query: |-
    dataset IN ("main","cribl_search_sample") | where auditType.category == "Projects"
    | where auditType.action == "Project secret scanning allowlist rule added"
tags:
  - sigma-import
  - bitbucket
  - audit
